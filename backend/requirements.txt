# TRACER backend — runtime dependencies
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
networkx>=3.3
numpy>=1.26
xgboost>=2.0.3
redis>=5.0
structlog>=24.1
prometheus-client>=0.19
pytest>=8.2
hypothesis>=6.100

# Optional hardening (install when needed in production):
# neo4j>=5.20            # persistent graph mirror (set TRACER_NEO4J_URI)
# shap>=0.46             # TreeSHAP parity checks
# torch torch_geometric  # GraphSAGE embeddings (set TRACER_USE_EMBEDDINGS=1)
